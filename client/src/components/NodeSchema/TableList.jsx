import React, { useState } from "react";
import {
  Box,
} from "@mui/material";

const TableList = ({
}) => {

  return (
    <Box
      sx={{
        width: "300px",
        height: "100%",
        overflowY: "auto",
        borderRight: "1px solid #ddd",
      }}
    >
    </Box>
  );
};

export default TableList;
